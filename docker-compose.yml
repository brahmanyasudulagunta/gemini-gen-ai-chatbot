services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      # Mapped to 6000 on the host, but assuming the application inside the
      # container is listening on the standard development port: 3000.
      - "6000:5171" 
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      # !! REPLACE "YOUR_ACTUAL_GEMINI_API_KEY" with your real key !!
      VITE_GEMINI_API_KEY: AIzaSyB--W5grgyM_oyVa4hikt5OQFbm59s-ZiU
